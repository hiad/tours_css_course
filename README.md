# tours_css_course
Udemy Course https://www.udemy.com/course/advanced-css-and-sass


This project is currently using a SASS compile and Live server as a simple server that will reload the page on any   css updates.

1. Run the CSS compiler

npm run compile:sass


2. Run the  live server

live-server